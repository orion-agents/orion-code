# ============================================================================
# Orion Code - 环境变量配置（已弃用 / DEPRECATED）
# ============================================================================
#
# 本项目的配置已迁移到 JSON 文件：~/.orion-code/orion.json
# 请参阅仓库内的模板：docs/orion.example.json
#
# 若仍需用环境变量覆盖配置，唯一受支持的前缀是 ORION_CODE_*（见
# src/product/environment.ts）。常用变量：
#   ORION_CODE_API_KEY          （必填；或用 providers[].apiKey 写在 orion.json）
#   ORION_CODE_API_BASE_URL
#   ORION_CODE_BASE_URL
#   ORION_CODE_MODEL
#   ORION_CODE_FALLBACK_MODEL
#   ORION_CODE_MODE             （development | production）
#   ORION_CODE_LOG_LEVEL        （debug | info | warn | error）
#   ORION_CODE_TOOL_CONFIRMATION（ask | allow | deny）
#   ORION_CODE_SUBAGENTS        （off | explicit | auto）
#   ORION_CODE_DEBUG_TOOLS
#   ORION_CODE_WEBSEARCH_API_KEY 等（web 搜索 MCP）
#
# 历史前缀说明（请勿使用）：
#   - REIN_*       : 已废弃，代码不再读取。
#   - OPENHORSE_*  : 仅用于旧配置迁移（LEGACY_ENV_MAP），正常运行时不被读取。
#
# 直接编辑全局配置即可，无需本文件：
#   ~/.orion-code/orion.json
# ============================================================================
