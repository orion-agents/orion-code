# OpenHorse 代码质量巡检报告

> **版本号**: v0.2.19（取自 package.json `version` 字段，每次运行重新读取）
> **检查时间**: 2026-07-16 09:19 (GMT+8)
> **累计发现问题数**: 第 14 次巡检确认待处理 **3 类**（P1×1 + P2×2）；自首次巡检累计记录 **8 项**，其中 **5 项源码 P0/P1 缺陷**经多轮核实持续保持已修复。
> **修复状态（2026-07-16 用户请求修复）**: ✅ P1 `better-sqlite3` ABI 已通过 `npm rebuild` 解决（sqlite 两套件 22 passed）；✅ P2 `launch.ts:165` 已补 `.catch/.finally`；🔶 P2 lint 警告已做**安全机械清理**（非 `any` 告警 96 → 4，仅剩 4 个 `prefer-const` 为确定性赋值绑定不可转 const；`no-explicit-any` 100 个按约定保留）。详见文末「六、修复记录」。
> **任务来源**: 定时巡检自动化（半点 / 每小时）
> **巡检方式**: 只读检查 + 本报告写文件；本轮应请求额外执行了源码修复（未提交 git、未删除文件）

---

## 一、命令执行结果摘要

| 检查项 | 命令 | 退出码 | 结果 |
|--------|------|--------|------|
| ESLint | `npm run lint` | 0 | ✅ 通过（**0 error / 201 warning**，与近 7 轮持平） |
| TypeScript 编译 | `npm run build` (`tsc`) | 0 | ✅ 通过（0 错误） |
| 单元测试（抽样） | `npx jest vector storage-maintenance terminal-ui-pty tui-ui-pty` | 1 | ❌ 2 套件失败 / 13 用例失败；根因为依赖原生模块 ABI 不匹配（`better-sqlite3`）；PTY 2 套件全绿（与全量历史一致） |
| 集成测试（PTY） | 同上抽样中的 PTY 套件 | 0 | ✅ 通过（`terminal-ui-pty` / `tui-ui-pty` 全部通过） |

**结论**：lint 与 build 命令本身均"通过"（退出码 0）。测试存在确定性失败，根因为**依赖原生模块 ABI 不匹配**（详见第二节 A）；本轮对**关键套件抽样**运行（better-sqlite3 依赖套件 + 2 个 PTY 套件），结果与历史全量运行一致：除 2 个 `better-sqlite3` 依赖套件外，其余套件（含抽样的全部 PTY 集成套件）全部通过，佐证其为环境噪声而非稳定源码缺陷。

**本轮增量说明**：`git` 自上一轮报告（08:26）以来**无任何 `src/` 源码提交**（最新 commit 仍为 `aa023e9` @ 02:02）。工作树 `git status` 显示 `src/` 无修改/未跟踪文件，源码与上一轮逐字节一致。因此源码级缺陷面与上一轮完全一致。

**Lint warning 构成（稳定 201）**：`no-explicit-any` 100、`no-unused-vars` 87、`prefer-const` 9、`ban-types` 4、`ban-ts-comment` 1。（注：本轮 lint 退出码为 0，warning 不阻断 CI。）

**测试细节**（Node 22.22.2 运行时，本轮抽样运行，耗时约 29s）：
- 抽样结果：`Test Suites: 2 failed, 2 passed, 4 total` / `Tests: 13 failed, 9 passed, 22 total`（抽样范围：`vector` + `storage-maintenance` + `terminal-ui-pty` + `tui-ui-pty`）。
- 确定失败的 2 套件为 `tests/vector.test.ts` 与 `tests/storage-maintenance.test.ts`，失败用例 13 个，根因均为 `better-sqlite3` ABI 不匹配（`new VectorStore` @ `src/memory/vector-store.ts:64` 加载原生二进制即抛 `NODE_MODULE_VERSION 137 vs 127`；`afterEach` 中 `store.close()` 因 `store` 未初始化而抛 `Cannot read properties of undefined (reading 'close')`）。
- 抽样的 2 个 PTY 集成套件（`terminal-ui-pty` 25.3s、`tui-ui-pty` 26.1s）本轮全部通过（2 套件 / 2 用例），与历史全量运行中全部 PTY 套件通过一致，进一步佐证 better-sqlite3 失败为环境噪声。

---

## 二、测试失败明细（根因分组）

### A. `better-sqlite3` 原生模块 ABI 不匹配【P1 · 环境/依赖 · 确定性】

- 现象：`node_modules/better-sqlite3/build/Release/better_sqlite3.node` 按 `NODE_MODULE_VERSION 137`（Node 24）编译；测试运行在 Node 22（ABI 127）上，Jest 的模块加载器优先命中该 `build/Release` 二进制 → `new Database()` 加载即抛 `Error: The module ... was compiled against a different Node.js version using NODE_MODULE_VERSION 137`。
- 影响套件（本轮 2 套件 / 13 用例全红）：
  - `tests/vector.test.ts`（`VectorStore` / `SemanticSearchService` 全部用例：初始化 `new VectorStore`、upsert、delete、search、`cleanupOrphanProjects`、默认路径、`afterEach` 中 `store.close()` 因 `store` 未初始化而抛 `Cannot read properties of undefined (reading 'close')` 等）
  - `tests/storage-maintenance.test.ts`（`cleanupStorage removes vector rows for orphan project keys` @99 等）
- 与历史结论一致：仅测试失败，**CLI 正常运行不受影响**（普通 `node -e "require('better-sqlite3')"` 在该 Node 22 下能解析到 ABI 127 兼容的 prebuild 二进制）。

### B. PTY / CLI 集成套件【P2 · 沙箱偶发 · 本轮未再现】

- 历史曾出现 `terminal-ui-pty` ↔ `tui-ui-pty` 漂移式失败（依赖真实伪终端，headless 沙箱偶发）。
- **本轮（09:19）全部 2 个抽样 PTY 相关套件通过**，进一步佐证其为环境噪声而非稳定源码缺陷。因仍可能在高负载/争用下偶发，保留为持续观察项。

---

## 三、静态扫描结果（边界与健壮性）

本轮对**全仓 `JSON.parse` / 类型断言 / 未处理 Promise 拒绝 / 数组访问**做了一致性复核。由于自上一轮以来 `src/` 无新提交，历史条目均持续保持；第 9 轮对 `web-search-mcp.ts:128` 的误报更正经本轮独立重读再次确认有效。

### `web-search-mcp.ts:128` 误报更正 ✅（第 9 轮撤回，本轮独立重读再次确认）

- 早期报告曾称 `parseJsonRpcMessages()` 内的 `const parsed = JSON.parse(text);`（line 128）"无 try-catch 包裹，会泄漏为未处理异常"。
- **本轮（09:19）独立重读确认**：该函数**仅有一个调用点** `src/services/web-search-mcp.ts:355`，且调用被 `try { return parseJsonRpcMessages(...) } catch (err: any) { throw new WebSearchMcpError('WEBSEARCH_MCP_PARSE_ERROR', ...) }`（lines 354–362）包裹。因此 `JSON.parse` 抛出的 `SyntaxError` 会被捕获并转为带上下文的 `WebSearchMcpError`，**不会成为未处理异常/未捕获崩溃**。另，`parseSseMessages()`（line 97）内的 `JSON.parse`（line 112）自身也有 `try-catch`（lines 111–115）忽略非 JSON 帧。**结论：无裸调用泄漏，原 P2 记录为误报，已移除。**

### JSON.parse 防护复核（全仓）

全仓 `JSON.parse` 共 64 处（与历史一致），绝大多数位于 `try-catch` 内或紧随 `.catch()`（`commands/index.ts`、`core/checkpoint.ts`、`services/session-storage.ts`、`tools/mcp.ts`、`services/llm.ts`、`runtime/chat-controller.ts` 等）。`web-search-mcp.ts:128` 的调用链经 line 355 的 `try-catch` 兜底。复核结论：**全仓 `JSON.parse` 防护一致，无源码级裸调用泄漏**。

### 未处理 Promise 拒绝扫描（`.then` 粗筛）

对 `src/` 全量 `.then(` 调用做粗筛，绝大多数位于已返回 Promise 或 executor 内（`resolve`/`reject` 已传递，错误可传播），无新增未处理拒绝。其中 `src/agents/worker-pool.ts:73/98` 的 `.then(resolve)` / `.then(result => ...)` 在 `new Promise` 内，拒绝经 executor 传播，历史已确认有 `.catch` 防护。**唯一独立的 `void ...then()` 无 `.catch` 仍为本轮记录的第 1 处 P2（`launch.ts:165`）**。

### 历史记录、本轮持续保持已修复的源码缺陷 ✅
| 原级别 | 位置 | 核实结果 |
|--------|------|----------|
| P0 | `src/runtime/agent-runtime-controller.ts`（`stopping` 不重置） | 已重置 `this.stopping = false`。✅ |
| P0 | `src/services/session-storage.ts`（JSONL 单行损坏丢全部历史） | 逐行 `try-catch`，跳过损坏行。✅ |
| P0 | `src/core/checkpoint.ts`（`meta.files` 未校验） | 有 `Array.isArray(meta.files)` 守卫。✅ |
| P1 | `src/harness/safety.ts`（用户配置正则无转义/预编译） | `safeCompileRegex` 预编译、超长(>500)/非法回退永不匹配正则（ReDoS 防护）。✅ |
| P1 | `src/agents/worker-pool.ts`（递归调用未 await） | 已有 `.catch()` 防护。✅ |

### 持续存在的 P2 缺陷（非本轮新增）
- **`src/tui-ui/launch.ts:165`**：`void cleanup().then(() => { finishLaunch(); });` 缺少 `.catch`：在 `failRenderer` 错误路径中若 `cleanup()` 拒绝，则 `finishLaunch()` 不执行且产生未处理 Promise 拒绝（unhandled rejection）。**本轮已重读确认仍原样存在（L165）**。

---

## 四、问题清单表格

| 严重度 | 文件 / 行号（或范围） | 问题描述 | 建议修复方向 |
|--------|----------------------|----------|--------------|
| **P1** | 依赖环境（`node_modules/better-sqlite3/build/Release`） | 原生模块按 Node 24（ABI 137）编译；Jest 在 Node 22（ABI 127）下优先加载该 `build/Release` 二进制 → `vector` / `storage-maintenance` 套件确定性全红（本轮 13 个用例失败） | 在测试运行所用的 Node 22 下执行 `npm rebuild better-sqlite3`；或统一测试 Node 大版本为 24（与已编译二进制一致）；或删除 `build/Release` 让其回退到 ABI 127 的 prebuild |
| **P2** | `src/tui-ui/launch.ts:165` | `void cleanup().then(() => { finishLaunch(); })` 缺少 `.catch`：在 `failRenderer` 错误路径中若 `cleanup()` 拒绝，则 `finishLaunch()` 不执行且产生未处理 Promise 拒绝（unhandled rejection） | 改为 `cleanup().then(finishLaunch).catch(finishLaunch)` 或 `try { await cleanup(); } finally { finishLaunch(); }` |
| **P2** | 全仓（ESLint 统计） | 201 个 warning：`@typescript-eslint/no-explicit-any` 100、`no-unused-vars` 87、`prefer-const` 9、`ban-types` 4、`ban-ts-comment` 1 | 分批治理，优先 `no-explicit-any` 与 `ban-ts-comment`；`prefer-const`/`no-unused-vars` 多数可 `eslint --fix` |

> 注：本轮未在 `src/` 中发现**新的 P0（崩溃/数据丢失）或 P1（功能异常）源码级缺陷**；唯一确定性测试失败为依赖 ABI 不匹配（环境/配置问题）。全部 PTY 集成套件本轮通过，作为潜在环境噪声持续观察。`web-search-mcp.ts:128` 误报（第 9 轮更正）经本轮独立重读再次确认。

> ⚠️ **版本标注偏差观察（非缺陷）**：`package.json` 的 `version` 仍为 `0.2.19`，但 `git log` 中近期提交均以 `v0.2.22`（Phase 3/4）为标签（如 `fix: validate contextWindow before casting (v0.2.22 P4-8)`、`fix: MCP reconnect timer cleanup + heartbeat unref (v0.2.22 P4-6/P4-7)`）。报告文件名依任务规则沿用 package.json 的 `v0.2.19`；建议同步 `npm version` 以避免发布标签与实际代码状态脱节。

---

## 五、建议

1. **优先修复依赖环境**：在运行测试的 Node 版本下对 `better-sqlite3` 执行 rebuild（或在 Node 24 下统一测试），使 `vector` / `storage-maintenance` 套件恢复绿色——这是当前唯一阻断测试确定性通过的问题。
2. **收敛未处理 Promise 拒绝**：为 `launch.ts:165` 的 `cleanup()` 调用补充 `.catch` / `finally`，确保错误路径也能走到 `finishLaunch()`。
3. **PTY 集成测试环境化**：相关套件本轮未失败但历史偶发，建议接入带真实伪终端的 CI，或在无 TTY 时自动 `skip`，消除沙箱偶发误报。
4. **Lint 治理**：201 个 warning 建议分批清理，优先 `no-explicit-any` 与 `ban-ts-comment`。
5. **同步版本号（建议）**：将 `package.json` 的 `version` 与 git 历史标签对齐（当前代码实际已含 v0.2.22 修复），避免巡检报告版本与代码演进脱节。
6. **源码质量稳定**：历史 5 项 P0/P1 缺陷持续保持已修复；全仓 `JSON.parse` 防护一致，无源码级新增缺陷；误报项（`web-search-mcp.ts:128`）已更正移除。

---

## 六、修复记录（2026-07-16 用户请求修复）

> 本轮在巡检之外，应请求实际执行了源码修复。所有改动均未提交 git、未删除文件。

### 1. P1 — `better-sqlite3` 原生模块 ABI 不匹配 ✅ 已修复
- 执行：`npm rebuild better-sqlite3`（当前 Node 22.22.2 / ABI 127 重新编译原生二进制）。
- 验证：`npx jest tests/vector.test.ts tests/storage-maintenance.test.ts` → **Test Suites: 2 passed, 2 total / Tests: 22 passed, 22 total**（此前为 13 failed / 9 passed）。
- 影响：仅解决测试运行环境，CLI 本就不受影响。

### 2. P2 — `src/tui-ui/launch.ts:165` 未处理 Promise 拒绝 ✅ 已修复
- 改动：原 `void cleanup().then(() => { finishLaunch(); });` 改为
  ```ts
  void cleanup()
    .catch(() => { emergencyRestore(); })
    .finally(() => { finishLaunch(); });
  ```
- 效果：错误路径必达 `finishLaunch()`，且 `cleanup()` 拒绝被 `.catch` 兜底，不再产生 unhandled rejection。
- 验证：`npm run build` 退出 0（0 错误）。

### 3. P2 — 全仓 ESLint 警告（201 → 104）🔶 安全机械清理完成
- 范围：按用户选择，仅做**低风险机械清理**，保留 `no-explicit-any`（100 个）。
- 结果：非 `any` 告警由 **96 → 4**。
  - `no-unused-vars`（87）：删除未使用导入/变量/死函数，未使用形参重命名为 `_*`。
  - `prefer-const`（4→4）：机械可转的已转；剩余 4 个为 `let x!: T` 确定性赋值/闭包重绑定（如 `print-ui/launch.ts:238 controller`、`terminal-ui/launch.ts:923 editor`、`:928 agentController`、`tools/index.ts:1015 timeoutId`），转 `const` 会编译失败，故保留。
  - `ban-types`（4）：`tools/lsp.ts:50`、`tools/mcp.ts:50` 的 `Function` → `(...args: unknown[]) => unknown`。
  - `ban-ts-comment`（1）：`ui/markdown.ts:12` `@ts-ignore` → `@ts-expect-error`（构建确认其下确有类型错误，保留）。
- 验证：`npm run build` 退出 0（0 错误）；`npx jest terminal-ui-pty tui-ui-pty` 2 套件全过（运行时确认编辑未破坏 `terminal-ui/launch.ts` / `tools/index.ts`）。
- 共改动 46 个源文件。剩余 104 个警告 = `no-explicit-any` 100 + `prefer-const` 4，均为非阻断项。

---

*本报告由代码质量巡检任务生成（半点自动复跑）。如发现误报或需调整检查粒度，请修改对应自动化任务 prompt。*
