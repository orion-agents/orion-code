# v0.2.24–v0.2.26 Integration Audit Report

**Date:** 2026-07-26
**Scope:** v0.2.24 (target mode), v0.2.25 (provider resilience), v0.2.26 (runtime integration)
**Status:** Modules complete, integration layer missing — features not end-to-end functional

---

## Executive Summary

All three versions share the same pattern: **module implementation is complete, but integration wiring is absent**. Code compiles and unit tests pass, but features are not functional end-to-end.

| Version | Data/Model Layer | Integration Layer | End-to-End Usability |
|---------|-----------------|-------------------|----------------------|
| v0.2.24 | Complete | Severely lacking | Command-line only; model cannot participate |
| v0.2.25 | Complete | Completely missing | Not usable |
| v0.2.26 | Complete | Completely missing | Not usable |

---

## Build & Test Status

- TypeScript compilation: **PASS**
- Test suite (138 suites, 1900 tests): **4 failed, 1895 passed**
- Current version: `0.2.26`

### Failing Tests

1. `session-consistency.test.ts` — `truncateSessionToLastComplete` messageCount mismatch (expected 4, got 2) — **real bug**
2. `terminal-ui-pty.test.ts` — PTY startup timeout (environment-related)
3. `tui-ui-pty.test.ts` — PTY startup timeout (environment-related)
4. `ink-ui-pty.test.ts` — PTY startup timeout (environment-related)

### Open GitHub Issues

- #73 — Completion Gate false-positive for non-code tasks
- #74 — openQuestions in Context Harness only accumulate, never clean up
- #75 — rootObjective does not update when user intent clearly shifts

---

## v0.2.24 — `/target` Persistent Goal Mode

### Delivered (Foundation Complete)

- Goal state machine (6 states: active, paused, blocked, usage_limited, budget_limited, complete)
- `/target` command parser (create/pause/resume/edit/replace/budget/clear)
- `GoalCoordinator` lifecycle management
- Atomic `.goal.json` sidecar persistence (CAS + corruption quarantine)
- Terminal UI command interception and rendering (PTY smoke passing)
- 5 test files, ~79 test cases

### Gaps

| # | Gap | Severity | File(s) |
|---|-----|----------|---------|
| G1 | Model tools (`get_goal`/`create_goal`/`update_goal`) **not registered** in model tool pipeline — model cannot participate in goal lifecycle | HIGH | `src/runtime/goals/tools.ts` |
| G2 | Goal prompt fragment **not injected** into prompt assembler — model never sees `[Persistent Goal]` context | HIGH | `src/runtime/goals/prompt.ts` |
| G3 | `finalizeTurn()` **never called** — accounting, no-progress detection, completion/blocked audits never trigger | HIGH | `src/runtime/agent-runtime-controller.ts` |
| G4 | TUI/Ink/Print renderers have **zero goal integration** — only Terminal UI works | HIGH | `src/tui-ui/`, `src/ui/`, `src/cli.ts` |
| G5 | Completion audit is stubbed — `evidenceRefs` defaults to empty, all completion requests fail | MEDIUM | `src/runtime/goals/coordinator.ts:239-241` |
| G6 | No integration tests, race condition tests, or renderer parity tests | MEDIUM | `tests/` |
| G7 | `AgentTurnOutcome` not produced by chat controller — `finalizeTurn()` cannot receive structured data | HIGH | `src/runtime/chat-controller.ts` |

---

## v0.2.25 — Provider Request Resilience

### Delivered (Module Layer Complete)

- V2 error taxonomy (21 FailureKinds, 5 RetryDispositions)
- Error classifier with status-code mapping and Retry-After parsing
- `ProviderResilienceCoordinator` — retry loop, backoff, AbortSignal
- `ProviderCircuitBreaker` — closed/open/half_open (implemented + tested)
- `StreamRecoveryEngine` — overlap reconciliation, semantic delta, partial tool call (implemented + tested)
- `ProviderRequestGate` — shared concurrency gate (implemented but not integrated)
- `ProviderRetryExhaustedError` — caught in chat-controller, returns to idle
- SDK `maxRetries: 0`
- `chat()` wired through optional `resilience` field

### Gaps

| # | Gap | Severity | File(s) |
|---|-----|----------|---------|
| R1 | `ProviderResilienceCoordinator` **never instantiated/injected** — resilience completely non-functional | HIGH | `src/services/provider-resilience/coordinator.ts` |
| R2 | `chatStream()` still uses old `withRetry()` — no retry/recovery/circuit-breaker for streaming | HIGH | `src/services/llm.ts:529` |
| R3 | `CircuitBreaker` **not wired** into `Coordinator.execute()` | HIGH | `src/services/provider-resilience/coordinator.ts` |
| R4 | `StreamRecovery` **not wired** into `chatStream()` | HIGH | `src/services/provider-resilience/stream-recovery.ts` |
| R5 | `ProviderRequestGate` **not bridged** with `SubagentProviderGate` — no shared cooldown | HIGH | `src/services/provider-resilience/request-gate.ts`, `src/runtime/subagents/provider-gate.ts` |
| R6 | Runtime Provider Events (7 variants) not implemented | MEDIUM | — |
| R7 | `compact_then_rebuild` disposition missing from architecture | MEDIUM | `src/services/provider-resilience/types.ts` |
| R8 | `x-should-retry` header not parsed | LOW | `src/services/provider-resilience/error-classifier.ts` |
| R9 | 11 planned test files missing | MEDIUM | `tests/` |

---

## v0.2.26 — Runtime Integration (Registry + ClientPool + ModelCoordinator)

### Delivered (Data/Config Layer Complete)

- `ModelRegistry` — multi-provider/model config, validation, resolution priority chain
- `ModelClientPool` — per-provider lazy client creation, env-var key resolution
- `ModelCoordinator` — prepare/commit/rollback model switching, model_changed event
- `config.ts` integration — loadConfig() builds Registry, exposes modelClientPool
- Legacy config migration hint
- Idle goal continuation / drift prevention / completion audit (code exists)
- `model-registry.test.ts` (18 cases, Slice 1)

### Gaps

| # | Gap | Severity | File(s) |
|---|-----|----------|---------|
| M1 | `ModelCoordinator` is **dead code** — never imported/instantiated anywhere | HIGH | `src/runtime/model-coordinator.ts` |
| M2 | `modelClientPool` on config has **no consumer** | HIGH | `src/services/config.ts` |
| M3 | `model_changed` event has **no listeners** | HIGH | `src/runtime/model-coordinator.ts` |
| M4 | `require()` in ES module context may fail at runtime | MEDIUM | `src/runtime/model-coordinator.ts:117` |
| M5 | No `model-coordinator.test.ts` | MEDIUM | — |
| M6 | No `model-client-pool.test.ts` | MEDIUM | — |
| M7 | No `completion-audit.test.ts` | MEDIUM | — |
| M8 | Preflight compact is a stub (flag set, no caller acts on it) | LOW | `src/runtime/model-coordinator.ts` |

---

## Fix Priority

### P0 — Must Fix (features non-functional without these)

1. **Instantiate `ProviderResilienceCoordinator`** and inject into Runtime; wire `chat()` and `chatStream()` through it; connect CircuitBreaker and StreamRecovery
2. **Register goal tools** in model tool pipeline; inject goal prompt fragment; call `finalizeTurn()` after turn completion; produce `AgentTurnOutcome`
3. **Wire `ModelCoordinator`** into Runtime; connect `/model` command; add model_changed event consumers

### P1 — Should Fix (significant gaps)

4. Bridge `ProviderRequestGate` with `SubagentProviderGate`
5. Fix `session-consistency` messageCount bug
6. TUI/Ink renderer goal integration
7. Fix completion audit evidence refs (remove `any` cast, add proper fields to `AgentTurnOutcome`)

### P2 — Nice to Have (test coverage, minor issues)

8. Add missing test files (14+ files)
9. Resolve GitHub Issues #73-75
10. Add Runtime Provider Events
11. Add `compact_then_rebuild` disposition
12. Parse `x-should-retry` header
