# Claude 继续执行清单（v0.2.14 收口）

## 先确认基线

- 分支：`v0.2.14`
- HEAD：`75ae886`（`feat(v0.2.14): complete remaining work — verification state, checkpoint policy, safety classification, error layering, and tool audit`）
- 当前状态：有大量工作已落盘，但目标仍未“收口”，核心闭环仍有 P0/P1/P2 待完成。

## 交接目标（给 Claude）

- **主目标**：不改 runtime 语义的前提下，把 `v0.2.14` 的稳定性闭环收全。
- **核心原则**：`terminal` 为主力 UI，`ink`/`tui` 为观察性 beta。runtime 决定行为；UI 只负责呈现。

## 绝对优先级（必须先做）

### P0-1：完成 completion/verification closure 与任务连续性
- 查 `docs/codex/v0.2.14-targets-remaining-work.md` 的条目 2（Runtime Completion Gate）和条目 3（Compact/Resume Continuity）。
- 关键文件：
  - `src/runtime/chat-controller.ts`
  - `src/runtime/agent-runtime-controller.ts`
  - `src/services/session-storage.ts`
  - `src/runtime/ui-events.ts`
- 要求：`changed-file` 任务必须有 `finishReason` + verification profile + 未通过/跳过说明，才能被当成完成。
- 关键用例：20+ 回合后 `/compact`，再输入 `继续`，必须能接上原 `rootObjective`。

### P0-2：工具事件一致性不能回退
- 重点修复：`toolFinished` 中 `skipped` 分支要保留真实 `name/args`，不能变成 `callId/空参数`。
- 文件：`src/runtime/chat-controller.ts`、`src/runtime/ui-events.ts`、`src/runtime/ui-view-model.ts`
- 检查点：`/trace` 可复现真实工具意图；`/session` 重放不出现断链。

### P0-3：终端长输出/长任务交互稳态
- 检查 `terminal-ui-pty` 场景是否覆盖：长流式输出、运行中输入修正、窗口 resize、中文输入、命令并发。
- 文件：`test-runtime/*`、`scripts/terminal-ui-pty-smoke.py`
- 不允许现象：输入光标跳动、内容覆盖、滚动历史被清空、状态行/横幅刷屏。

## P1 继续收敛

### P1-1：安全闭环可见化（已部分接入，继续核验）
- `classifyCommandSafety` 与 `isRiskyEdit` 要和执行链一致；风险提示需和 checkpoint/permission 输出一致。
- 文件：`src/services/verification-profile.ts`、`src/core/checkpoint.ts`、`src/runtime/chat-controller.ts`、`src/runtime/ui-view-model.ts`
- 输出要求：高风险命令、multi-file 修改有清晰 rollback 指引且与是否成功落库一致。

### P1-2：完整命令与长参数可追溯
- 命令截断时要保证有稳定查看入口：`/last-tool` 或 `artifacts`。
- 文件：`src/runtime/ui-events.ts`、`src/runtime/ui-view-model.ts`

### P1-3：Prompt 组装可观测
- 每次模型回合要保留 `promptAssemblyStats`（含 omitted/included evidence、预算压力）用于 `/harness explain`。
- 文件：`src/harness/*`、`src/runtime/chat-controller.ts`、`src/runtime/ui-view-model.ts`

## P2 提升（可并行）

- 工具 scheduler 的 read-only 批量化、顺序保序展示继续校验。
- renderer parity：`terminal` 行为为准线，`ink/tui` 的功能回归只做非破坏性验证。
- 把 `docs/targets/ui-ultimate-experience.md`、`docs/targets/agent-loop-final-form.md`、`docs/targets/ui-runtime-boundary.md` 与 codex remaining-work 文档统一到“当前可验收清单”。

## 明确禁止项（不要改）

- 不要把 agent-loop、session、harness、permission、安全策略搬进 `terminal`/`ink`/`tui` UI。
- 不要在未验证前把 `ink` 提升为默认 UI。
- 不要为 UI 美观暂时绕过 `scrollback-first`、`resume`、`compact` 的真实行为。

## Claude 接入后先做的动作（按顺序）

1. 运行 `npm run build`。
2. 运行 `npm test -- --runInBand tests/runtime-ui-parity.test.ts tests/terminal-ui.test.ts`。
3. 运行 `npm test -- --runInBand tests/agent-runtime-controller.test.ts tests/runtime-ui-view-model.test.ts tests/harness.test.ts`。
4. 本机手工验收：
   - 长输出 + 打字修正；
   - Ctrl+C 首次中断、再次退出；
   - `/resume` 后继续 `继续`；
   - 中文输入法候选词与 Backspace。

## 交接附录

- 读：
  - `docs/codex/v0.2.14-targets-remaining-work.md`
  - `docs/codex/v0.2.14-ui-ultimate-gap-plan.md`
  - `docs/targets/ui-runtime-boundary.md`
  - `docs/targets/ui-ultimate-experience.md`
  - `docs/targets/agent-loop-final-form.md`
- 相关事件点位：
  - `AgentRuntimeEvent.type = session_restore / tool_started / tool_finished / permission_requested / completion_gate`。
