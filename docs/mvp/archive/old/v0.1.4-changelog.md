# OpenHorse v0.1.4 Roadmap - 工具系统扩展 & MCP 协议支持

## Context

基于对 OpenClaude 的深度调研，v0.1.4 聚焦于扩展工具系统，引入关键缺失能力：

1. **网络访问工具** - WebFetch/WebSearch，获取外部信息
2. **MCP 协议支持** - 标准化工具协议，扩展生态
3. **TodoWrite 工具** - 任务追踪，改进用户体验
4. **PlanMode 工具** - 复杂任务规划模式
5. **Bash 安全增强** - 沙箱执行，安全防护

---

## OpenClaude vs OpenHorse 工具对比

| 功能 | OpenClaude | OpenHorse 当前 | v0.1.4 目标 |
|------|-----------|---------------|-------------|
| 文件操作 | FileRead/FileEdit/FileWrite/Glob/Grep | ✅ read_file/write_file/edit_file/glob/grep | 保持 |
| Shell 执行 | BashTool (沙箱+安全验证) | ✅ exec_command (基础) | 增强 |
| Memory 系统 | ✅ memdir/ | ✅ memory/ | 保持 |
| 网络访问 | WebFetch/WebSearch | ❌ | ✅ 新增 |
| MCP 协议 | MCPTool/ListMcpResources | ❌ | ✅ 新增 |
| 任务追踪 | TodoWrite | ❌ | ✅ 新增 |
| 规划模式 | EnterPlanMode/ExitPlanMode | ❌ | ✅ 新增 |
| Fork Subagent | forkSubagent | ❌ | P2 |
| LSP 支持 | LSPTool | ❌ | P2 |

---

## Part 1: 网络访问工具 (P0)

### 1.1 WebFetch 工具

**功能**: 获取 URL 内容并用 prompt 处理

**参考**: `/Users/hope/ai-project/openclaude/src/tools/WebFetchTool/`

**实现方案**:

```typescript
// src/tools/web.ts
buildTool({
  name: 'web_fetch',
  description: 'Fetch content from a URL and process with a prompt',
  parameters: {
    type: 'object',
    properties: {
      url: { type: 'string', description: 'URL to fetch' },
      prompt: { type: 'string', description: 'Prompt to process content' },
    },
    required: ['url', 'prompt'],
  },
  execute: async (args) => {
    const response = await fetch(args.url);
    const content = await response.text();
    
    // Convert HTML to markdown (basic)
    const markdown = htmlToMarkdown(content);
    
    // Apply prompt (use LLM or simple extraction)
    const result = await applyPrompt(markdown, args.prompt);
    
    return { success: true, output: result };
  },
  isReadOnly: () => true,
});
```

**关键设计**:
- 预审批域名列表 (github.com, docs.google.com 等)
- 内容大小限制 (MAX_MARKDOWN_LENGTH)
- 错误处理 (404, timeout)

### 1.2 WebSearch 工具

**功能**: 网络搜索获取信息

**参考**: `/Users/hope/ai-project/openclaude/src/tools/WebSearchTool/`

**实现方案**:

```typescript
// src/tools/web.ts
buildTool({
  name: 'web_search',
  description: 'Search the web for information',
  parameters: {
    type: 'object',
    properties: {
      query: { type: 'string', description: 'Search query' },
      limit: { type: 'number', description: 'Max results (default 5)' },
    },
    required: ['query'],
  },
  execute: async (args) => {
    // 使用搜索 API (需要配置)
    // 或者使用 DuckDuckGo API (免费)
    const results = await searchWeb(args.query, args.limit || 5);
    return { success: true, output: formatResults(results) };
  },
});
```

**配置**:
```bash
OPENHORSE_SEARCH_PROVIDER=duckduckgo  # 或 google, bing
OPENHORSE_SEARCH_API_KEY=xxx
```

---

## Part 2: MCP 协议支持 (P0)

### 2.1 MCP 概述

**MCP (Model Context Protocol)** 是 Anthropic 定义的标准化工具协议：
- 工具定义 JSON Schema
- 工具调用标准格式
- 资源访问接口

**参考**: `/Users/hope/ai-project/openclaude/src/tools/MCPTool/`

### 2.2 MCP Tool 实现

```typescript
// src/tools/mcp.ts
interface MCPToolDefinition {
  name: string;
  description: string;
  inputSchema: object;  // JSON Schema
}

interface MCPClient {
  listTools(): Promise<MCPToolDefinition[]>;
  callTool(name: string, args: object): Promise<string>;
}

buildTool({
  name: 'mcp_call',
  description: 'Call an MCP tool from connected MCP servers',
  parameters: {
    type: 'object',
    properties: {
      server: { type: 'string', description: 'MCP server name' },
      tool: { type: 'string', description: 'Tool name to call' },
      args: { type: 'object', description: 'Tool arguments' },
    },
    required: ['server', 'tool', 'args'],
  },
  execute: async (args) => {
    const client = getMCPClient(args.server);
    const result = await client.callTool(args.tool, args.args);
    return { success: true, output: result };
  },
});

buildTool({
  name: 'mcp_list',
  description: 'List available MCP tools from connected servers',
  parameters: { type: 'object', properties: {} },
  execute: async () => {
    const tools = await listAllMCPTools();
    return { success: true, output: formatToolList(tools) };
  },
});
```

### 2.3 MCP Server 配置

```json
// ~/.openhorse/mcp.json
{
  "servers": {
    "filesystem": {
      "command": "mcp-server-filesystem",
      "args": ["--root", "/Users/hope/projects"]
    },
    "github": {
      "command": "mcp-server-github",
      "env": { "GITHUB_TOKEN": "xxx" }
    }
  }
}
```

---

## Part 3: TodoWrite 工具 (P1)

### 3.1 功能设计

**参考**: `/Users/hope/ai-project/openclaude/src/tools/TodoWriteTool/`

**用途**: 任务追踪，显示进度

```typescript
// src/tools/todo.ts
interface TodoItem {
  content: string;
  status: 'pending' | 'in_progress' | 'completed';
  activeForm: string;  // "正在实现..."
}

buildTool({
  name: 'todo_write',
  description: 'Update the task todo list',
  parameters: {
    type: 'object',
    properties: {
      todos: {
        type: 'array',
        items: {
          type: 'object',
          properties: {
            content: { type: 'string' },
            status: { type: 'string', enum: ['pending', 'in_progress', 'completed'] },
            activeForm: { type: 'string' },
          },
        },
      },
    },
    required: ['todos'],
  },
  execute: async (args) => {
    // Store todos in AppState
    ctx.store.setState({ todos: args.todos });
    
    // Print todo list to console
    printTodoList(args.todos);
    
    return { success: true, output: 'Todo list updated' };
  },
});
```

### 3.2 UI 显示

```
[1. ✅ completed] P0: WebFetch 工具
[2. ⏳ in_progress] P1: TodoWrite 工具
[3. ⬚ pending] P2: PlanMode 工具
```

---

## Part 4: PlanMode 工具 (P1)

### 4.1 功能设计

**参考**: `/Users/hope/ai-project/openclaude/src/tools/EnterPlanModeTool/`

**用途**: 复杂任务进入规划模式，探索后生成计划

```typescript
// src/tools/plan.ts
buildTool({
  name: 'enter_plan_mode',
  description: 'Enter plan mode for complex tasks requiring exploration',
  parameters: { type: 'object', properties: {} },
  execute: async (args) => {
    ctx.store.setState({ planMode: true });
    console.log('Entered plan mode. Use /plan to create plan, /exit_plan to leave.');
    return { success: true };
  },
});

buildTool({
  name: 'exit_plan_mode',
  description: 'Exit plan mode and return to execution',
  parameters: {
    type: 'object',
    properties: {
      plan: { type: 'string', description: 'Implementation plan' },
    },
    required: ['plan'],
  },
  execute: async (args) => {
    ctx.store.setState({ planMode: false, currentPlan: args.plan });
    return { success: true };
  },
});
```

---

## Part 5: Bash 安全增强 (P1)

### 5.1 安全验证

**参考**: `/Users/hope/ai-project/openclaude/src/tools/BashTool/bashSecurity.ts`

**增强点**:
- 危险命令检测 (rm -rf, mkfs, dd)
- 沙箱模式选项
- 路径验证 (防止路径遍历)
- 只读命令预审批

```typescript
// src/tools/bash_security.ts
const DANGEROUS_PATTERNS = [
  /rm\s+-rf\s+\/$/,          // 删除根目录
  /mkfs/,                    // 格式化
  /dd\s+of=\/dev/,           // 磁盘写入
  /:\(\)\s*\{/,              // fork bomb
];

const READ_ONLY_COMMANDS = [
  'ls', 'cat', 'head', 'tail', 'grep', 'find', 'git status',
  'git log', 'git diff', 'npm list', 'node --version',
];

function isReadOnlyCommand(cmd: string): boolean {
  const baseCmd = cmd.split(' ')[0];
  return READ_ONLY_COMMANDS.includes(baseCmd);
}

function checkDangerousCommand(cmd: string): { safe: boolean; reason?: string } {
  for (const pat of DANGEROUS_PATTERNS) {
    if (pat.test(cmd)) {
      return { safe: false, reason: `Blocked dangerous pattern: ${pat}` };
    }
  }
  return { safe: true };
}
```

---

## Part 6: Skills 系统 (P2)

### 6.1 功能设计

**参考**: `/Users/hope/ai-project/openclaude/src/skills/`

**用途**: 可扩展技能，用户自定义功能

```typescript
// src/skills/loader.ts
interface SkillDefinition {
  name: string;
  description: string;
  prompt: string;
  tools?: string[];  // 可用工具列表
}

function loadSkills(projectPath: string): SkillDefinition[] {
  const skillsDir = join(projectPath, '.openhorse', 'skills');
  // 加载 skill 定义文件
}
```

---

## 文件修改清单

```
src/tools/
├── index.ts              # 整合新工具 (P0)
├── web.ts                # WebFetch/WebSearch (NEW, P0)
├── mcp.ts                # MCP Tool (NEW, P0)
├── todo.ts               # TodoWrite (NEW, P1)
├── plan.ts               # PlanMode (NEW, P1)
├── bash_security.ts      # Bash 安全增强 (NEW, P1)

src/services/
├── mcp-client.ts         # MCP 客户端 (NEW, P0)
├── web-client.ts         # 网络请求封装 (NEW, P0)

src/skills/
├── loader.ts             # Skills 加载器 (NEW, P2)
├── types.ts              # Skills 类型 (NEW, P2)

src/framework/
├── store.ts              # AppState 扩展 (todos, planMode)
```

---

## 实现优先级

### P0 (必须)
- [x] WebFetch 工具
- [x] WebSearch 工具
- [x] MCP Tool 基础支持
- [x] MCP Server 配置

### P1 (重要)
- [x] TodoWrite 工具
- [x] EnterPlanMode/ExitPlanMode 工具
- [x] Bash 安全增强
- [ ] 沙箱模式

### P2 (可选)
- [ ] Fork Subagent 支持
- [ ] Skills 系统加载器
- [ ] LSP Tool 支持
- [ ] Team memory 共享
- [ ] Memory embeddings search

---
## 实现说明 (2026-05-18)

### 已完成文件

```
src/tools/
├── web.ts           # WebFetch + WebSearch (P0 ✅)
├── mcp.ts           # mcp_list + mcp_call (P0 ✅)
├── todo.ts          # todo_write (P1 ✅)
├── plan.ts          # enter_plan_mode + exit_plan_mode (P1 ✅)
├── bash_security.ts # Bash 安全增强 (P1 ✅)
├── index.ts         # 所有工具注册
```

### 工具列表

| 工具名 | 功能 | 状态 |
|--------|------|------|
| web_fetch | 获取 URL 内容 | ✅ |
| web_search | DuckDuckGo 搜索 | ✅ |
| mcp_list | 列出 MCP 工具 | ✅ |
| mcp_call | 调用 MCP 工具 | ✅ |
| todo_write | 任务追踪 | ✅ |
| enter_plan_mode | 进入规划模式 | ✅ |
| exit_plan_mode | 退出规划模式 | ✅ |

### Bash 安全增强 (P1 ✅ 2026-05-21)

**实现文件**: `src/tools/bash_security.ts`

**功能**:
- `READ_ONLY_COMMANDS` - 只读命令预审批列表 (ls, cat, git status, npm list 等)
- `DANGEROUS_PATTERNS` - 危险命令模式 (rm -rf /, mkfs, dd of=/dev, fork bomb 等)
- `isReadOnlyCommand()` - 检查命令是否为只读
- `checkDangerousCommand()` - 检查命令是否危险
- `assessCommandSecurity()` - 综合安全评估
- `wrapForSandbox()` - 沙箱命令包装 (docker/bubblewrap)

**exec_command 权限检查更新**:
- 只读命令自动允许
- 危险命令自动拒绝
- 潜在破坏性命令需确认

---

## 验收标准

### 网络工具
- [ ] `/chat 获取 https://example.com 的内容` 能正常工作
- [ ] `/chat 搜索 TypeScript 教程` 返回搜索结果
- [ ] 预审批域名无需确认

### MCP 协议
- [ ] 配置 MCP server 后能列出工具
- [ ] 能调用 MCP 工具并返回结果
- [ ] 支持 filesystem/github 等 MCP server

### Todo/Plan
- [ ] TodoWrite 更新任务列表显示
- [ ] PlanMode 进入后不执行，只探索
- [ ] ExitPlanMode 保存计划并返回执行模式

### 安全增强
- [x] 危险命令被阻止
- [x] 只读命令预审批
- [ ] 沙箱模式可选启用

---

## 测试方案

### 1. 网络工具测试

```bash
openhorse
> /chat 获取 https://github.com/anthropics/claude-code 的 README
# 应返回 README 内容

> /chat 搜索 "OpenAI API documentation"
# 应返回搜索结果
```

### 2. MCP 测试

```bash
# 配置 MCP server
cat ~/.openhorse/mcp.json
{"servers": {"test": {"command": "echo", "args": ["hello"]}}}

openhorse
> /chat 列出 MCP 工具
# 应显示 test server 的工具

> /chat 调用 test server 的 echo 工具
# 应返回 hello
```

### 3. Todo/Plan 测试

```bash
openhorse
> /chat 更新任务列表：完成 WebFetch 工具
# 应显示任务列表

> /chat 进入规划模式
# 应提示已进入规划模式

> 探索项目结构
# 不执行代码，只探索

> /chat 退出规划模式，计划是：实现 WebFetch 工具
# 应保存计划并退出
```

---

## 参考

**OpenClaude 源码**:
- `src/tools/WebFetchTool/` - WebFetch 实现
- `src/tools/WebSearchTool/` - WebSearch 实现
- `src/tools/MCPTool/` - MCP 协议实现
- `src/tools/TodoWriteTool/` - TodoWrite 实现
- `src/tools/EnterPlanModeTool/` - PlanMode 进入
- `src/tools/BashTool/bashSecurity.ts` - Bash 安全
- `src/skills/` - Skills 系统