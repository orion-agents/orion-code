# OpenHorse v0.1.3 Roadmap - Session 存储修复 & Memory 项目维度

## 背景

v0.1.3 包含两个核心改进：

1. **Session 存储修复** — tool_calls 数据缺失导致对话恢复断裂
2. **Memory 改为项目维度** — 与 OpenClaude 保持一致，每个项目独立记忆

---

## Part 1: Session 存储修复

### 问题分析

**当前存储格式（缺失关键信息）**:
```json
{"role":"assistant","content":"","timestamp":1778759247935,"toolCallId":"call_xxx"}
{"role":"tool","content":"{\"success\":true,\"output\":\"...\"}","timestamp":1778759247943,"toolCallId":"call_xxx"}
```

**缺失**:
- `tool_calls[].function.name` — 工具名
- `tool_calls[].function.arguments` — 调用参数

**LLM API 需要的格式**:
```json
{
  "role": "assistant",
  "content": "",
  "tool_calls": [
    {
      "id": "call_xxx",
      "type": "function",
      "function": {
        "name": "read_file",
        "arguments": "{\"path\":\"src/cli.ts\"}"
      }
    }
  ]
}
{
  "role": "tool",
  "content": "{\"success\":true,\"output\":\"...\"}",
  "tool_call_id": "call_xxx"
}
```

### 修复方案

#### 1.1 扩展 SessionMessage 类型

**文件**: `src/services/session-storage.ts`

```typescript
export interface ToolCallRecord {
  id: string;
  type: 'function';
  function: { name: string; arguments: string };
}

export interface SessionMessage {
  role: 'user' | 'assistant' | 'system' | 'tool';
  content: string;
  timestamp: number;
  toolCallId?: string;
  tool_calls?: ToolCallRecord[];  // 新增
}
```

#### 1.2 修改 handleChat 记录逻辑

**文件**: `src/commands/index.ts`

```typescript
let pendingToolCalls: ToolCallRecord[] = [];

case 'tool_call':
  pendingToolCalls.push({
    id: event.callId,
    type: 'function',
    function: {
      name: event.name,
      arguments: JSON.stringify(event.args),
    },
  });
  break;

case 'message':
  sessionMessagesToRecord.push({
    role: 'assistant',
    content: event.content,
    timestamp: Date.now(),
    tool_calls: pendingToolCalls.length > 0 ? pendingToolCalls : undefined,
  });
  pendingToolCalls = [];
  break;
```

#### 1.3 修改 loadSessionHistory

```typescript
export function loadSessionHistory(sessionId: string): Message[] {
  const messages = readSessionMessages(sessionId);
  return messages.map(m => ({
    role: m.role,
    content: m.content,
    ...(m.toolCallId ? { tool_call_id: m.toolCallId } : {}),
    ...(m.tool_calls ? { tool_calls: m.tool_calls } : {}),
  }));
}
```

---

## Part 2: Memory 项目维度重构

### 当前问题

OpenHorse 当前 memory 存储在 `~/.openhorse/memory/`（全局），与 OpenClaude 不一致。

**OpenClaude 设计**:
```
/Users/hope/.claude/projects/<project-path-hash>/memory/
  ├── MEMORY.md
  ├── user_role.md
  ├── feedback_style.md
  └── project_goals.md
```

### 新设计

**路径结构**:
```
~/.openhorse/projects/<project-path-hash>/memory/
  ├── MEMORY.md              # 索引文件
  ├── *.md                   # 记忆文件
```

**路径计算**:
```typescript
// 项目路径转为 hash（避免路径中的特殊字符）
import { createHash } from 'crypto';

function getProjectHash(projectPath: string): string {
  return createHash('sha256').update(projectPath).digest('hex').slice(0, 16);
}

// 例如: /Users/hope/ai-project/openhorse → a1b2c3d4e5f6g7h8
```

### 修改方案

#### 2.1 修改 memory/storage.ts

```typescript
import { createHash } from 'crypto';

/** 获取项目 hash */
export function getProjectHash(projectPath: string): string {
  return createHash('sha256').update(projectPath).digest('hex').slice(0, 16);
}

/** 获取项目的 memory 目录 */
export function getMemoryDir(projectPath?: string): string {
  const configHome = getConfigHome();  // ~/.openhorse
  
  if (projectPath) {
    const hash = getProjectHash(projectPath);
    return join(configHome, 'projects', hash, 'memory');
  }
  
  // 兼容：无 projectPath 时使用全局（v0.1.2 风格）
  return join(configHome, 'memory');
}

/** 确保项目 memory 目录存在 */
export function ensureProjectMemoryDir(projectPath: string): string {
  const dir = getMemoryDir(projectPath);
  if (!existsSync(dir)) {
    mkdirSync(dir, { recursive: true });
  }
  return dir;
}
```

#### 2.2 CLI 启动时加载项目 memory

**文件**: `src/cli.ts`

```typescript
import { loadAllMemories, getMemoryDir } from './memory/storage';

async function main(): Promise<void> {
  const projectPath = process.cwd();
  
  // 加载项目 memory
  const memories = loadAllMemories(projectPath);
  const memoryContent = memories.length > 0 
    ? memories.map(m => `## ${m.name}\n${m.content}`).join('\n\n')
    : '';
  
  store = new Store({
    config: cliConfig,
    tools: TOOLS,
    currentModel: cliConfig.model,
    memoryContent,  // 新增
  });
}
```

#### 2.3 PromptContext 传递 memory

**文件**: `src/commands/index.ts`

```typescript
async function handleChat(ctx: CommandContext, input: string): Promise<CommandResult> {
  const promptCtx: PromptContext = {
    cwd: ctx.cwd,
    platform: process.platform,
    nodeVersion: process.version,
    tools: TOOLS,
    memoryContent: ctx.store.getSnapshot().memoryContent,  // 使用项目 memory
  };
  const systemPrompt = getSystemPrompt(promptCtx);
}
```

#### 2.4 Memory 工具使用项目路径

**文件**: `src/tools/index.ts`

```typescript
buildTool({
  name: 'memory_save',
  execute: async (args) => {
    const projectPath = process.cwd();  // 当前项目
    saveMemory({
      name: args.name,
      type: args.type,
      content: args.content,
      description: args.content.slice(0, 100),
    }, projectPath);  // 传入项目路径
    return { success: true, output: `Saved memory: ${args.name}` };
  },
});

buildTool({
  name: 'memory_recall',
  execute: async (args) => {
    const projectPath = process.cwd();
    const memories = args.query 
      ? searchMemories(args.query, projectPath)
      : loadAllMemories(projectPath);
    return { success: true, memories };
  },
});
```

---

## 文件修改清单

```
src/services/session-storage.ts  # SessionMessage 类型扩展 (P0)
src/commands/index.ts            # handleChat 记录完整 tool_calls (P0)
src/memory/storage.ts            # 项目维度路径计算 (P0)
src/memory/types.ts              # 无需修改
src/cli.ts                       # 加载项目 memory (P0)
src/tools/index.ts               # memory 工具传入项目路径 (P0)
```

---

## 实现优先级

### P0 (Session 存储)
- [x] SessionMessage 类型扩展（tool_calls 字段）
- [x] handleChat 记录完整 tool_calls
- [x] loadSessionHistory 返回完整 Message 格式

### P0 (Memory 项目维度)
- [x] getProjectHash 路径计算
- [x] getMemoryDir(projectPath) 支持
- [x] CLI 启动加载项目 memory
- [x] memory 工具使用项目路径

### P1 (增强功能)
- [x] handleResume 显示历史摘要
- [x] history_search 工具
- [x] Session 元数据增强（taskSummary）

---

## 验收标准

### Session 存储
- [x] assistant 消息包含完整 `tool_calls`
- [x] `/resume` 恢复后 LLM 能理解之前的工具调用
- [x] 恢复后不重复调用已完成的工具

### Memory 项目维度
- [x] 不同项目有独立的 memory 目录
- [x] CLI 启动加载当前项目的 memory
- [x] memory_save/recall 工具操作当前项目 memory
- [x] 切换项目后 memory 内容不同

---

## 测试方案

### 1. Session 存储验证

```bash
# 运行对话，包含工具调用
openhorse
> /chat 读取 src/cli.ts 前 10 行

# 检查存储
cat ~/.openhorse/sessions/<id>.jsonl | jq 'select(.role=="assistant") | .tool_calls'

# 应输出完整结构
```

### 2. Memory 项目维度验证

```bash
# 项目 A
cd /Users/hope/ai-project/openhorse
openhorse
> 保存记忆：这个项目用 TypeScript

# 项目 B
cd /Users/hope/ai-project/ai-note
openhorse
> 搜索记忆：TypeScript
# 应返回空（不同项目）

> 保存记忆：这个项目是 Markdown 笔记库

# 回到项目 A
cd /Users/hope/ai-project/openhorse
openhorse
> 搜索记忆：TypeScript
# 应找到之前保存的记忆
```

### 3. 恢复验证

```bash
openhorse
> /resume
# 应显示历史摘要
> 我之前读取了什么文件？
# 应正确回答：src/cli.ts
```

---

## 参考

**OpenClaude memory 路径**:
- `src/memdir/memdir.ts` — `getMemoryDir()` 使用项目路径
- `src/utils/project.ts` — `getProjectHash()` 计算路径 hash
- `.claude/projects/<hash>/memory/` — 存储结构