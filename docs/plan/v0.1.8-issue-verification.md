# v0.1.8 Issue Verification Ledger

> Live issue snapshot: 2026-08-14
> Source baseline: `v0.1.8` / `52b6fc23c6ae9b1a210dcd4de29ca751f8a1bb06`
> Candidate-fix ancestor: `682e85821f7361097a6081d6e95235717224ded2`
> Rule: local acceptance is not a GitHub closure until the exact commit is pushed and linked.

## Current classification

| Issue | Classification                                        | Acceptance evidence                                                                                                                               |
| ----- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| #170  | Local acceptance passed; delivery pending             | Cross-process input-history barrier in `global-config.test.ts`                                                                                    |
| #171  | Local acceptance passed; delivery pending             | Failed Goal binding clear preserves the Goal in `goal-tools.test.ts`                                                                              |
| #172  | Local acceptance passed; delivery pending             | 5,000-row transcript operation-count regression in `session-storage.test.ts` proves incremental append does not rescan JSONL                      |
| #173  | Local acceptance passed; delivery pending             | Exact pack/hash/fresh install/CLI/native checks in `release-check.js` and `release-scripts.test.ts`                                               |
| #174  | Local acceptance passed; delivery pending             | Finite search limit and fetch/axios/MCP byte budgets in `bounded-response`, `web-search-mcp`, and service branch suites                           |
| #175  | Product-contract mismatch; close after delivery       | AUTO remains prompt-free after hard policy/explicit deny; network matrix, visible status/tool authorization, and architecture decision added      |
| #176  | Local acceptance passed; delivery pending             | Unreadable owner fail-closed for generic and Goal locks in `file-lock-recovery` and `goal-sidecar`                                                |
| #177  | Local acceptance passed; delivery pending             | ESC/C0/C1/OSC lifecycle sanitization across Terminal/Print/Goal renderers                                                                         |
| #178  | Local acceptance passed; delivery pending             | Published `npm-shrinkwrap.json`, clean tarball install, production tree/native checks                                                             |
| #179  | PR #201 behavior integrated locally; delivery pending | Renderer gate runs before special command handling; `renderer-command-scope.test.ts`                                                              |
| #180  | Local acceptance passed; delivery pending             | 5,000 retained entries × 1,000 live updates with zero prefix scans in `tui-render-scheduler.test.ts`                                              |
| #181  | Local acceptance passed; delivery pending             | Missing/malformed `npm pack` metadata is a blocking failure                                                                                       |
| #182  | Local acceptance passed; delivery pending             | File and parent-directory symlink escapes create no checkpoint artifact                                                                           |
| #183  | Local acceptance passed; delivery pending             | Read/write Git operations reject absolute and symlinked cwd outside `ToolContext.cwd`                                                             |
| #184  | Local acceptance passed; delivery pending             | Stale summary snapshot reconciles from the locked current transcript                                                                              |
| #185  | Local acceptance passed; delivery pending             | Multiline byte and line-count budgets cancel/reset with actionable `/edit` guidance                                                               |
| #186  | Local acceptance passed; delivery pending             | Heartbeat, idle, and terminate share the per-session lock and cannot recreate a terminated slot                                                   |
| #187  | Local acceptance passed; delivery pending             | Metadata-keyed instruction cache avoids unchanged rereads and invalidates changed/new rules                                                       |
| #189  | Local acceptance and PTY passed; delivery pending     | Self-terminating objective contract plus real TUI/Terminal failure, repair, compact, restart, resume, audited completion, and automatic Goal exit |
| #190  | Local acceptance passed; delivery pending             | Trace append/delete share the session lock; stale append cannot recreate deleted trace                                                            |
| #191  | Local acceptance passed; delivery pending             | Session/model picker fields and headers strip terminal controls                                                                                   |
| #192  | Local acceptance passed; delivery pending             | Dependency gate asserts manifest override and resolved shrinkwrap tree                                                                            |
| #193  | Local acceptance passed; delivery pending             | Per-provider cooldown isolation plus diagnostic tracker/formatter coverage                                                                        |
| #194  | Local acceptance passed; delivery pending             | `flush()` resolves from queue-drain notification without `setTimeout(0)` polling                                                                  |
| #195  | Local acceptance passed; delivery pending             | Resize listener attaches only while visible and disposes without duplication                                                                      |
| #196  | Local acceptance passed; delivery pending             | Shared terminal sanitizer covers palette, suggestions, file completion, width helpers                                                             |
| #197  | Local acceptance passed; delivery pending             | CI dependency policy is blocking; network report remains separate/advisory                                                                        |
| #198  | Local acceptance passed; delivery pending             | Built-in name snapshots remain isolated across repeated registrations                                                                             |
| #199  | Local acceptance passed; delivery pending             | read/list/glob/grep reject absolute, traversal, and symlink escape roots                                                                          |
| #200  | Local acceptance passed; delivery pending             | Invalid JSON/argument shapes fail before tracking, permission, or execution                                                                       |
| #203  | Local acceptance passed; delivery pending             | Shared safe-integer validation and table-driven scheduler/tool boundary tests                                                                     |
| #204  | Local acceptance passed; delivery pending             | Typed budget-stop view with stop point, usage, `继续`, narrower task, `/loop-stats`, config action                                                |
| #161  | Release operation; no code closure                    | Re-query dist-tags after the exact v0.1.8 tarball passes every gate; promotion requires release authorization                                     |

## Focused evidence recorded so far

- Security/input batch: 13 suites, 340 tests passed on Node 24.14.1.
- Persistence/performance/release batch: 19 suites, 488 tests passed; `goal-evidence` had one
  cross-suite transient and then passed 28/28 in an isolated rerun. The final full-suite run is the
  deciding gate.
- Numeric tool validation: 4 suites, 238 tests passed.
- Budget-stop parity: 6 suites, 204 passed and 1 skipped.
- Renderer scope: 2 suites, 5 tests passed.
- AUTO network contract and authorization rendering/provenance focused suites passed.
- Session incremental append: `session-storage.test.ts`, 58 tests passed, including a 5,000-row
  transcript with zero transcript reads during append.
- Real PTY matrix passed for BUILD/PLAN/AUTO, Goal lifecycle and automatic exit, TUI CJK input,
  terminal IME, print mode, research/subagent renderer parity, `/target` compatibility, and the
  deprecated Ink renderer.
- Final Node 24 serial Jest gate passed 244 suites with 1 skipped and 3816 tests with 6 skipped;
  the earlier Goal cross-suite transient did not recur.
- Exact package gate passed at 1,223 files, 1.41 MiB packed, and 6.39 MiB unpacked with one runtime
  asset plus clean install/version/help/native probes.
- Isolated `npm ci`, build, CLI, dependency tree, better-sqlite3, and sqlite-vec probes passed on
  Node 20.20.2 (ABI 115), Node 22.22.3 (ABI 127), and Node 24.14.1 (ABI 137).

## Required delivery closure

1. Pass lint, build, full serial Jest, prepublish, exact tarball install, PTY matrix, and Node
   20/22/24 checks.
2. Commit and push the exact tested tree; record remote SHA.
3. Link the commit/PR and exact commands on each issue before closing.
4. Close #175 as a documented product-contract mismatch and retire PR #201 through one canonical
   integration path rather than leaving two competing diffs.
5. Leave #161 open until a separately authorized tag/npm/GitHub Release/dist-tag operation is
   complete.
