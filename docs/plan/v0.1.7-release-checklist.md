# v0.1.7 Release Readiness Checklist

> Scope: publish the reviewed v0.1.7 candidate after explicit user authorization on 2026-08-13.
> Source merge, release tag, GitHub prerelease, npm publication, and registry installation remain
> separate evidence states until their immutable receipts are recorded here.

## Delivery-state ledger

| State                  | Status      | Required evidence                                                |
| ---------------------- | ----------- | ---------------------------------------------------------------- |
| Source candidate       | PASS        | `6239367`; local gates, exact-head CI, candidate tarball probes   |
| Merged to `main`       | PASS        | PR #188; merge `cdbddea`                                         |
| Git tag `v0.1.7`       | IN PROGRESS | annotated tag must dereference to the final release commit       |
| GitHub prerelease      | NOT_RUN     | release URL, tag, tarball checksum assets                        |
| npm `0.1.7` published  | NOT_RUN     | public registry metadata and tarball integrity read-back         |
| Registry clean install | NOT_RUN     | exact public version on Node 20, 22, and 24                      |
| npm `next`             | `0.1.6`     | target `0.1.7`; explicit publish authorization received          |
| npm `latest`           | `0.1.4`     | unchanged; stable promotion remains a separate explicit decision |

## Candidate gates

- Package: `@orion-agents/orion-code@0.1.7`; supported Node matrix: 20, 22, 24.
- Node 24.14.1: dependency policy, zero-vulnerability audit, ESLint, TypeScript, and build pass.
- Node 24.14.1 full coverage: 236 suites / 3714 tests pass; 1 suite / 6 tests are conditionally
  skipped; global statement/branch/function/line coverage remains above the configured 70% gate.
- Candidate exact-head CI passed twice (push and PR): Node 20/22/24, dependency audit, TypeScript,
  dependency health, and `release:check`
  ([run 31657466544](https://github.com/orion-agents/orion-code/actions/runs/31657466544),
  [run 31657514712](https://github.com/orion-agents/orion-code/actions/runs/31657514712)).
- A clean committed checkout must produce the real tarball. The release gate must validate its
  manifest, and separate clean-prefix installs must verify CLI identity plus `better-sqlite3` and
  `sqlite-vec` on Node 20, 22, and 24 before npm publication.
- The 31 MB of unreferenced local concept/promo images and the zero-byte `events.append` runtime
  artifact are not part of the release commit or npm tarball. The existing runtime icon remains.
- The fresh open-issue audit found no issue carrying a P0/P1 priority label. Follow-up issues
  #170-#187 remain public and do not become silently closed by this release; security/reliability
  findings are called out in prerelease notes and must be evaluated before a stable `latest`
  promotion.

## Release order

1. Push the candidate branch and open a PR to `main`.
2. Require exact-head Node 20/22/24, audit, typecheck, dependency-health, and release-gate CI.
3. Merge the approved PR and commit final release metadata on `main`.
4. Create and atomically push annotated `v0.1.7` from that release commit.
5. Publish `@orion-agents/orion-code@0.1.7 --tag next` from the exact tagged tarball.
6. Read back registry integrity and clean-install the public package on Node 20/22/24.
7. Create the GitHub prerelease with the tarball, SHA256SUMS, validation summary, and known issues.
8. Record all immutable receipts in a post-release documentation commit.

## Rollback

- npm versions and Git tags are immutable; never overwrite or force-move `v0.1.7`.
- If publication succeeds but verification fails, deprecate `0.1.7`, restore `next` to `0.1.6`,
  keep the GitHub prerelease visible with the failure note, and prepare a new patch version.
- Stable `latest` is unchanged by this release and therefore needs no rollback.
