# 源码优先质量轮训：下一轮任务清单（2026-08-14）

- 轮训频率：每 30 分钟。
- 执行模式：每轮只聚焦单一维度；未检测到问题时写“本轮未复现问题”。
- 自动轮次：安全与输入校验 -> 权限与边界控制 -> 持久化/并发一致性 -> TUI 交互与终端输出安全 -> 依赖/版本与供应链风险 -> 测试完整性与测试实现一致性 -> 性能与回归风险 -> Release/发布工程风险。

## 上一轮结论

- 上一轮执行维度：**安全与输入校验**。
- 发现问题：`src/tools/index.ts` 的 `exec_command` 与 `list_files` 参数透传缺少边界校验。
- 处理：官方仓库已提问题，见 [Issue #203](https://github.com/orion-agents/orion-code/issues/203)，并已加 `security` / `testing` 标签。

## 下一轮待办（本次新增）

1. 在本轮后续工作中补充**优化项**：
   - 对 `exec_command` 的 `timeout`、`maxOutput` 做上/下界校验与类型收敛，防止异常值触发资源耗尽或异常行为。
   - 对 `list_files` 的 `maxDepth` 加上下界/上界约束与类型收敛，避免负数、过大值、非数值导致递归或遍历风险放大。
   - 补充对应源级回归用例，优先覆盖拒绝路径：非法数值、越界、非数值类型。
   - 将优化项绑定到本仓库源码级执行，作为“安全与输入校验”后续追踪项继续落库。

2. 下一轮轮训维度候选（按规则自动推进）：
   - **权限与边界控制**。
