# v0.1.9 release checklist

Status: `candidate`; immutable source SHA and delivery receipts are filled only after the relevant
action succeeds.

## Identity and scope

- [ ] `package.json` and `npm-shrinkwrap.json` both identify `0.1.9`.
- [ ] `node scripts/release/release-check.js --expected-version 0.1.9` passes on the exact RC SHA.
- [ ] Original user-owned dirty documentation is excluded from the release commit.
- [ ] CHANGELOG, README files, migration guide, goals, architecture benchmark, and this checklist
      describe the same candidate state.

## Issue and workstream receipts

- [ ] #206: independent usage-ledger lock, invalid-number fail-closed behavior, lock timeout
      diagnostic, and real multi-process replay test.
- [ ] #207: canonical containment, symlink/non-regular rejection, descriptor revalidation,
      replacement-race regression, and redacted errors.
- [ ] #161: explicit decision is `next` candidate only unless the user separately authorizes
      `latest`; keep the issue open while stable promotion remains pending.
- [ ] Semantic Compact: ten-round invariant retention, tool-group integrity, bounded focus,
      provider fallback, model-switch preflight, stale-CAS rejection, crash rollback, restart/resume.
- [ ] Autonomous Stop: request/Goal/subagent StopDecision parity, 21+ real Goal continuations,
      no-progress pause, resource pause, abort/shutdown, and Print no-ghost behavior.
- [ ] HarnessKernel: TaskContract V3, criterion evidence graph, capability versions, deterministic
      serialization, atomic prompt budgets, opaque evidence rejection, and explain output.

## Source gates

- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `npm test -- --runInBand`
- [ ] `npm run test:coverage -- --runInBand`; total coverage does not regress and new core modules
      meet the documented branch target.
- [ ] `npm run prepublishOnly`
- [ ] `git diff --check`
- [ ] worktree clean at the release SHA

## Runtime and compatibility matrix

- [ ] Node 20: clean install, build, full serial Jest, native DB probe, version/help.
- [ ] Node 22: clean install, build, full serial Jest, native DB probe, version/help.
- [ ] Node 24: clean install, build, full serial Jest, native DB probe, version/help.
- [ ] TUI BUILD/PLAN/AUTO/Goal smoke.
- [ ] Terminal, Print text/JSONL, Research, resume/Goal, and Compact lifecycle smoke.
- [ ] Exact `npm pack` tarball installs into an empty prefix and runs version/help/doctor plus a
      compact/resume smoke.

## Delivery receipts

| Receipt               | Expected evidence                         | Result                           |
| --------------------- | ----------------------------------------- | -------------------------------- |
| Source SHA            | immutable commit on `v0.1.9`              | pending                          |
| Pull request          | URL, head SHA, CI conclusion              | pending                          |
| Git tag               | separately authorized `v0.1.9` target     | not authorized by this checklist |
| GitHub Release        | separately authorized release URL         | not authorized by this checklist |
| npm immutable version | `@orion-agents/orion-code@0.1.9` readback | pending                          |
| npm `next`            | dist-tag readback equals `0.1.9`          | pending                          |
| npm `latest`          | separate explicit authorization           | keep at `0.1.4`                  |

Rollback: do not infer one receipt from another. If npm publication fails, leave `next` on the
last verified version. Follow [the migration guide](../migration/v0.1.8-to-v0.1.9.md) for
data-compatible executable rollback.
