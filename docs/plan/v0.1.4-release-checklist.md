# v0.1.4 Release Checklist — Research-to-Evidence

> Companion to `docs/plan/v0.1.4-plan.md`. Every external-state item defaults to
> `not_run` until real CI / package / registry evidence exists. Local tests are
> NOT release completion.

## Implementation phases

| Phase | Scope | Status | Evidence |
| ----- | ----- | ------ | -------- |
| P0-R1 | Research schema + local-only contract | ✅ done | `tests/research-contract.test.ts` (8) |
| P0-R2 | Controlled web research adapter | ✅ done | `tests/web-research-adapter.test.ts` (9) |
| P0-R3 | Citation / conflict / evidence candidate | ✅ done | `tests/research-citation.test.ts` (8) |
| P1-R4 | Shared runtime + TUI/Print parity | ✅ done | `tests/research-renderer-parity.test.ts` (3) |
| P1-R5 | Artifact / resume / Goal integration | ✅ done | `tests/research-artifact.test.ts` (6) |
| P1-R6 | Quality / docs / cost hardening | ✅ done | `tests/research-quality.test.ts` (4) + README + this file |

## Pre-publish commands (run under Node 24, safe-delete guard disabled)

```bash
git diff --check
npm run build
npm run lint
npm test -- --runInBand
npm test -- --runInBand --coverage
npm pack --dry-run
node scripts/release-check.js
```

## External-state evidence ledger

| Target | Status | How to verify |
| ------ | ------ | ------------- |
| Local source check | ✅ met | in-scope files only, no junk staged |
| Staged / committed source | ✅ met | `git log --oneline v0.1.3..v0.1.4` |
| CI / review | `not_run` | open PR to `main` |
| Merge / tag / GitHub Release | `not_run` | create `v0.1.4` after merge |
| npm publish | `not_run` | `npm publish` after tag + `prepublishOnly` green |
| Post-publish registry install | `not_run` | `npm view @orion-agents/orion-code@0.1.4` + clean-install smoke |

## Go / No-Go

### Go conditions

- R1–R6 all have focused test evidence (above).
- No-write / no-generic-MCP / SSRF / body-limit negative tests all pass.
- ResearchPacket, Goal evidence, and renderer schema have compatible/rollback notes (this file + plan §11).
- Docs do not present experimental/partial sources as verified or complete.

### No-Go conditions (any triggers a block)

- Web content can change system prompt, tool permission, or Goal state.
- Child gains write / exec / generic MCP / recursive subtask.
- A claim without a source binding can reach `verified`/`complete`.
- Source failure / provider fallback / timeout / cancellation is silently dropped.
- Packet crosses project / session / Goal scope, or resume overwrites an active Goal.
- Only local dirty worktree or stale test results, no final source/CI/package evidence.

## Scope isolation (freeze boundary)

In-scope (commit to v0.1.4): `src/runtime/subagents/research-*.ts` + `tests/research-*.test.ts`
+ `README*` research section + this checklist.

Excluded (must NOT enter v0.1.4): `AGENTS.md` (env markers), `.DS_Store`, `.claude/`,
`scripts/__pycache__/*`, `skills-lock.json`, `docs/img` / `docs/mvp/archive` deletions,
`src/framework/tool-scheduler.ts` (independent hardening, separate concern), runtime artifacts.
