# v0.1.8 Repository Cleanup Manifest

> Baseline: `v0.1.8` at `52b6fc23c6ae9b1a210dcd4de29ca751f8a1bb06`
> Audit date: 2026-08-14
> Recovery: every removal can be restored from the baseline commit with a path-scoped Git restore.

## Evidence rules

All candidates below were tracked, had no production/public import, and were excluded only after
an `rg` reference audit. Local ignored assets are not part of this cleanup and remain untouched.

## Unreferenced documentation images

`docs/img/` contained 18 tracked PNG files (about 21.2 MiB). No Markdown, HTML, package entry,
runtime source, test, or script referenced a concrete file. The directory was historical generated
artwork, not release evidence. It is removed rather than copied into the archive; the npm runtime
icon remains at `assets/orion-tui-icon.png`.

## Legacy runtime scripts

The old `test-runtime/` scripts were manual, post-build probes with no package script, CI workflow,
or maintained documentation entry. Their deterministic replacements are:

| Removed path              | Maintained replacement                                                   |
| ------------------------- | ------------------------------------------------------------------------ |
| `capture-pty.py`          | `scripts/smoke/tui-ui-pty-smoke.py`, `tests/tui-ui-pty.test.ts`          |
| `feed-raw.ts`             | `tests/tui-ui-pty.test.ts`, `tests/ansi-parser.test.ts`                  |
| `tui-repro.ts`            | `scripts/smoke/tui-ui-pty-smoke.py`, `tests/tui-inline-surface.test.ts`  |
| `test-brain.js`           | `tests/issue32.test.ts`, `tests/api-compatibility.test.ts`               |
| `test-exec-command.js`    | `tests/tools.test.ts`, `tests/exec-command-security-integration.test.ts` |
| `test-git-tools.js`       | `tests/git-tools.test.ts`                                                |
| `test-harness.js`         | `tests/harness.test.ts`, `tests/harness-safety.test.ts`                  |
| `test-memory-store.js`    | `tests/memory-store.test.ts`, `tests/memory-validation.test.ts`          |
| `test-relevant-finder.js` | `tests/v0.1.11.test.ts`, `tests/issue-fixes-round13.test.ts`             |
| `test-sdk-exports.js`     | `tests/api-compatibility.test.ts`                                        |
| `test-team-paths.js`      | `tests/v0.1.11.test.ts`, `tests/issue-fixes-round13.test.ts`             |
| `test-token-detection.js` | `tests/v0.1.11.test.ts`, `tests/redaction-sk-key.test.ts`                |
| `test-vector.js`          | `tests/vector.test.ts`, `tests/vector-store-sqlite-vec.test.ts`          |
| `test-web-search.js`      | `tests/web-search-provider.test.ts`, `tests/web-search-mcp.test.ts`      |
| `test-web-ssrf.js`        | `tests/web-ssrf-blocklist.test.ts`, `tests/web-ssrf-bypass.test.ts`      |

## Retired UI directories

| Removed path       | Audit result                                                                | Maintained owner                                                        |
| ------------------ | --------------------------------------------------------------------------- | ----------------------------------------------------------------------- |
| `src/ink/index.ts` | No production, public-export, or test import                                | `src/ink-ui/` compatibility renderer and `src/tui-core/` primitives     |
| `src/ui-v2/`       | Only `tests/ui-v2.test.ts` and one legacy coverage import; no runtime entry | `src/runtime/ui-view-model.ts`, `src/tui-ui/`, shared `src/ui/` helpers |

The unique v2 picker/input behaviors are covered by current command/file/session/edit-preview state,
layout, input, and renderer suites. The v2-only test file and the obsolete edit-preview branch in
the legacy Ink coverage suite are removed with the source.

## Verification checklist

- No remaining import or documentation entry references a removed path.
- Package scripts and CI reference the new `scripts/release`, `scripts/smoke`, and
  `scripts/maintenance` locations.
- Build, lint, full Jest, renderer PTY matrix, document-link audit, and exact tarball install pass.
- `npm pack --dry-run --json` contains exactly one runtime asset and no removed/generated artwork.
