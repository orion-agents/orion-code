# v0.3.8 代码质量目标（tech-team targets 模式）

- 日期：2026-09-03
- 分支：`codex/v0.3.8`（worktree `.worktrees/v0.3.8`，基 496560a = v0.3.7 merged）
- 前置已完成：todo 死代码移除、goals exclude 收据契约调查（保留）、全仓回归 308/309、lint/format 门禁对称化（PR #246 内 commit）

## Objective

在已发布 v0.3.7 基线上，把「代码质量检查与修复」收敛到 100% —— 每个验收项以可复现证据闭合，禁止"尽力了"式结论。

## Acceptance（evidence 驱动）

- **A1 v0.3.7 新代码边界幂等**：reducer 生命周期 action 对重复/未知输入幂等
  - evidence：`npx jest tests/web-reducer-session-lifecycle.test.ts`（≥16 例全绿，新增重复归档/未知删除用例）
- **A2 会话归档/还原存储层并发/幂等**：archive 已归档会话不二次改、restore 未归档 no-op 语义正确
  - evidence：`npx jest tests/session-storage-tags-archive.test.ts` 全绿（含幂等追加用例）
- **A3 新 DOM 无重复静态 id / 裸 aria**（v0.3.7 web 新组件面）
  - evidence：`tests/web-ui-*.tsx` 渲染断言 + 静态复核（SessionRowMenu hint useId 已修；无其它重复 id 字面量）
- **A4 工作树清洁 + 门禁**：lint / format / tsc（root+web+e2e）0 错误；无探针/临时残留
  - evidence：命令输出；`git status --short` 仅意图内改动
- **A5 发布物一致**：release:check --skip-tests 全绿（dist 构建后）
  - evidence：命令输出 ✅ 清单
- **A6 交付收据**：CHANGELOG [0.3.8] 与 goal doc 反映本轮全部处置（含 reviewer 否决项）
  - evidence：git diff

## Non-goals（Reviewer 否决/环境边界，记录理由）

- server.ts 四条 session 路由 DRY 重构 —— 结构同形且每条约 25 行，抽取 helper 引入间接层净收益为负（if-chain 一致性更易审），**否决**，仅记录
- tests/ 顶层历史 322 lint 违规全量修复 —— 存量、非本版引入；网已对新文件生效
- 沙箱不可跑套件（controller 重型集成 / web e2e / PTY / sqlite）—— 由 PR #246 CI 承接
- goals/* tsconfig 排除恢复 —— ArchitectureConfluence 收据契约要求，保留（曾验证后回滚）

## Completion audit (2026-09-03)

| # | 状态 | 证据 |
|---|------|------|
| A1 | ✅ met | `web-reducer-session-lifecycle.test.ts` **18 例绿**（+4 幂等：重复归档不重复插 / 未知归档 no-op / 未知删除 no-op / restore 不重复顶插） |
| A2 | ✅ met | `session-storage-tags-archive.test.ts` **7 例绿**（+2：重复归档单一态、restore 未归档 no-op） |
| A3 | ✅ met | 静态复核：新组件面无 map 内静态 id（SessionRowMenu 已 useId）；Dialogs/ProjectNavigator 的 id 均为单实例容器；web-ui 渲染断言覆盖 |
| A4 | ✅ met | lint 0 / format:check 0 / tsc（root+web+e2e）0；`git status` 仅意图文件（测试幂等用例 + goal doc） |
| A5 | ✅ met | release:check --skip-tests 8/8 ✅（dist 构建后，含 npm package artifact） |
| A6 | ✅ met | 本文件 + CHANGELOG [0.3.8]（回归清扫段已含）；non-goals 记录理由 |

Non-goal 复核结论不变：路由 DRY 否决（净收益负）；tests/ 历史 322 lint 违规为存量（新文件网已生效）；goals exclude 属收据契约保留。

## Constraints

- 不自动 merge/发布；改动静默最小化；PTY/原生绑定套件不本地跑
