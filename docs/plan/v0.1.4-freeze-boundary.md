# v0.1.4 Freeze Boundary — Research-to-Evidence

> Companion to `v0.1.4-plan.md` and `v0.1.4-release-checklist.md`. Records exactly
> which files are in-scope for the v0.1.4 release commit set, and which local /
> environment residue must stay OUT of the release.

## In-scope (committed to v0.1.4)

| Commit | Files | Phase |
| ------ | ----- | ----- |
| (1) | `src/runtime/subagents/research-types.ts`, `research-contract.ts`, `tests/research-contract.test.ts` | P0-R1 |
| (2) | `src/runtime/subagents/web-research-adapter.ts`, `tests/web-research-adapter.test.ts` | P0-R2 |
| (3) | `src/runtime/subagents/research-citation.ts`, `tests/research-citation.test.ts` | P0-R3 |
| (4) | `src/runtime/subagents/research-renderer.ts`, `tests/research-renderer-parity.test.ts` | P1-R4 |
| (5) | `src/runtime/subagents/research-artifact.ts`, `tests/research-artifact.test.ts` | P1-R5 |
| (6) | `src/runtime/subagents/research-quality.ts`, `tests/research-quality.test.ts`, `README.zh-CN.md`, `README.md`, `docs/plan/v0.1.4-release-checklist.md` | P1-R6 |

Test evidence: **38 passing** across 6 research test files (Node 24, safe-delete
guard disabled for the build/test step only).

## Excluded residue (must NOT enter v0.1.4)

- `AGENTS.md` (env-injected markers) — never commit
- `.DS_Store`, `.claude/`, `scripts/__pycache__/*`, `skills-lock.json`
- `docs/img/*.png` deletions, `docs/mvp/archive/*` deletions
- `src/framework/tool-scheduler.ts` (independent ToolRisk/allowlist hardening — a
  separate security concern, NOT Research scope)
- runtime artifacts (`.orion-code-*`, smoke-script local edits)

## Branch topology

- `v0.1.4` branched from `v0.1.3` tip (`02c4872`), because `main` has not yet
  merged `v0.1.3` (PR #4 still OPEN).
- `git push origin HEAD:refs/heads/v0.1.4` (branch shares no name collision with a
  tag, unlike `v0.1.3`).

## Release status

- Push + PR to `main`: done (reversible).
- Tag `v0.1.4` + `npm publish`: **`not_run` (gated)** — Research-to-Evidence is
  marked experimental; the plan's Go/No-Go requires CI / package / registry
  evidence that is not yet produced in this environment. Do NOT publish until the
  checklist's external-state ledger is filled.
