# Orion Code v0.1.2 执行计划

> 状态：Active（Phase 0 进行中）
>
> 目标版本：`@orion-agents/orion-code@0.1.2`
>
> 发布基线：`@orion-agents/orion-code@0.1.1`（main HEAD `567eb65`，npm shasum `732e7a7b`）
>
> 上位文档：[项目级目标](../goals/orion-code-项目级目标.md) §7 阶段 A
>
> 主题：让 v0.1.1 已存在但未接通的 Goal 骨架真正端到端工作（单 Session、单 Active Goal）

本计划由项目级目标 §6.3 现实状态层驱动，不依赖历史 MVP 草案。所有 P0 要求映射到
代码位置、测试入口和验收证据。

## 1. 基线与范围

### 1.1 v0.1.1 基线（已验证可重现）

- npm：`@orion-agents/orion-code@0.1.1`，shasum `732e7a7b7532df3c848fe552ad9a0e3c6808dbaa`
- main HEAD：`567eb65`（PR #2 merge）
- v0.1.2 分支：`v0.1.2`，基线 commit `0f1b54e`（goals 文档刷新）

### 1.2 已知基线缺口

- **v0.1.1 未打 git tag**（DoD §9.2 要求 tag）。不追溯补打；v0.1.2 发布时必须 tag。
- v0.1.1 的 3 个 PTY smoke 测试在本地基础设施超时（非代码 bug），记为已知问题。

### 1.3 范围归属（dirty worktree）

- `docs/img/` 的 PNG 删除与「已生成图像」PNG **不属于 v0.1.2**（视觉素材，非 goal 系统）。
- v0.1.2 只动 `src/runtime/goals/*`、`src/runtime/agent-runtime-*`、`src/runtime/ui-*`、
  `src/runtime/chat-controller.ts`、`src/harness/*`、`src/services/goal-storage.ts`、
  `src/tui-ui/*`、`src/terminal-ui/launch.ts` 及对应测试与文档。

### 1.4 范围约束（patch 版本）

- 单 Session、单 Active Goal；不做多目标、跨 Session 调度、unattended daemon。
- sidecar 只做 additive 扩展，v0.1.1 sidecar 必须可读；破坏性 schema 升级延期 v0.2。
- 不新增公众 slash commands，不扩大默认权限，不自动执行发布/删除/外部高风险操作。
- Print/JSON 仍标记 experimental。

## 2. Requirement -> Code -> Test -> Evidence 矩阵

行号基于 v0.1.1 基线代码，实现时可能漂移；以函数名为准。

### P0-1 结构化 Goal Turn（取代 marker 字符串）

| 要求 | 当前断点 | 实现位置 | 测试入口 | 验收证据 |
|---|---|---|---|---|
| runtime 直接消费 `AgentTurnRequest`，不提交 marker 字符串 | `handleTargetInput` (`agent-runtime-controller.ts:227`) 与 `scheduleGoalContinuation` (`:359`) 丢弃 typed request，提交 `[goal continuation #N]` | `agent-runtime-controller.ts`、`coordinator.ts:buildContinuationRequest` | `tests/goal-runtime-e2e.test.ts`(新)、`tests/agent-runtime-controller.test.ts` | 每个 continuation 携带 goalId/revision/continuationIndex；marker 不出现在 transcript 或持久 user message |
| `persistAsUserMessage=false`、`echoToTranscript=false` | 同上 | 同上 | 同上 | continuation 不写入 user transcript |
| generation/revision 取消 guard | pause/interrupt/replace/shutdown 后旧 callback 仍可能重启 | `agent-runtime-controller.ts` scheduler | `tests/goal-runtime-e2e.test.ts` | pause/replace/shutdown 后零 ghost continuation；stale goalId/revision 被拒绝 |

### P0-2 逐 criterion 证据与 Completion Audit

| 要求 | 当前断点 | 实现位置 | 测试入口 | 验收证据 |
|---|---|---|---|---|
| `GoalContract`（originalObjective + successCriteria[] + planSnapshot）additive 扩展 | `SessionGoalV1` (`types.ts:72`) 只有单一 `objective` 字符串 | `goals/types.ts`、`goal-storage.ts` | `tests/goal-contract.test.ts`(新) | v0.1.1 sidecar 无损加载并 normalize 为最小 pending contract |
| `update_goal complete\|blocked` 记录绑定当前 turn/goalId/revision 的 terminal request | `updateGoalTool` (`tools.ts:108`) 只回文本 ack | `goals/tools.ts`、`coordinator.ts` | `tests/goal-evidence.test.ts`(新) | tool success 只表示"请求已接收"，不改变终态 |
| `finalizeGoalTurn` 在 outcome 上填充 evidence/verification，使 `finalizeTurn` audit 分支可达 | `finalizeGoalTurn` (`agent-runtime-controller.ts:378`) 从不填充 `pendingTerminalRequest`/`evidenceRefs` | `agent-runtime-controller.ts`、`coordinator.ts:finalizeTurn`(`:235`) | `tests/completion-audit.test.ts` | audit 分支被触发；缺证据时 Goal 保持 active |
| 逐 criterion audit：每个 required criterion 需新鲜、相关、可追溯 evidence | `auditCompletion` (`completion-audit.ts:18`) 只查 `evidenceRefs.length>0 && summary.length>0` | `goals/completion-audit.ts`、`harness/evidence.ts`(新) | `tests/completion-audit.test.ts`、`tests/goal-evidence.test.ts` | 无关/失败/过期 evidence 不能关闭 criterion；每个 passed criterion 有可追溯 evidence |
| 证据账本：模型只能引用已捕获证据，不能凭文本自封 passed | 无证据捕获机制 | `harness/evidence.ts`(新)、`goals/tools.ts` | `tests/goal-evidence.test.ts` | 模型自然语言/自评不能作为 passed evidence |
| `update_goal blocked` 需同一 blocker 连续 3 个 goal turn + 无进展 | `auditBlocked` (`completion-audit.ts:34`) 已有 3 turn 门槛但 no-progress 判断不足 | `goals/completion-audit.ts` | `tests/goal-blocked-audit.test.ts`(新) | blocker 变化或不足 3 turn 时保持 active/paused |

### P0-3 Session Resume 与 Coordinator 隔离

| 要求 | 当前断点 | 实现位置 | 测试入口 | 验收证据 |
|---|---|---|---|---|
| `/resume` 重绑被恢复 session 的 GoalCoordinator 并加载 sidecar | `sessionRestored` (`launch.ts:584`) 只输出状态行，不创建/加载 coordinator | `terminal-ui/launch.ts`、`agent-runtime-controller.ts:ensureGoalCoordinator`(`:598`) | `tests/goal-resume.test.ts`(新) | resume 后展示恢复的目标；不读取上一个 session 的 coordinator |
| 移除模块级单例 coordinator，按 runtime/session scope 注入 | `tools.ts:21` 模块级 `_coordinator`，每次新 session 覆盖 | `goals/tools.ts` | `tests/goal-resume.test.ts` | session switch 后 tool 指向新 coordinator |
| 移除 `*` 通配符 session 匹配 | `setGoalCoordinator` (`agent-runtime-controller.ts:110`) 设 `goalCoordinatorSessionId='*'` 短路匹配 | `agent-runtime-controller.ts` | `tests/goal-resume.test.ts` | 跨 session 无 coordinator 泄漏 |
| `SessionMeta.activeGoalId` 成为活字段 | `session-storage.ts:116` 死字段，从不读写 | `services/session-storage.ts`、`goal-storage.ts` | `tests/goal-storage-compat.test.ts`(新) | resume 能从 session meta 定位 goal |
| process restart -> 安全 `paused/recovery_required`，显式 `/target resume` 才继续 | 无 restart 恢复逻辑 | `coordinator.ts`、`agent-runtime-controller.ts` | `tests/goal-resume.test.ts` | restart 后无静默工具调用；需显式 resume |

### P0-4 Goal Events 接入 shared runtime

| 要求 | 当前断点 | 实现位置 | 测试入口 | 验收证据 |
|---|---|---|---|---|
| `UiEventSink` + `AgentRuntimeEvent` 增加 goal event 方法与类型 | `UiEventSink` (`ui-events.ts:226`) 无 goal 方法；`goal_*` 仅 `types.ts:198` 类型 | `ui-events.ts`、`agent-runtime-protocol.ts:73` | `tests/runtime-ui-parity.test.ts` | goal event 类型与方法编译通过 |
| controller 是 goal events 唯一发射源 | 零发射点 | `agent-runtime-controller.ts` | `tests/goal-parity.test.ts` | `goal_updated/restored/continuation/audit_failed/completed/cleared` 从 controller 发射 |
| view-model 投影；TUI 显示 status/进度/预算/next action/audit | view-model 无 goal 投影 | `ui-view-model.ts`、`tui-ui/*` | `tests/tui-*.test.ts` | TUI resume 后展示恢复目标；audit 失败显示缺失 criterion |
| terminal-ui 文本 parity；renderer 切换不改 goal 状态 | terminal-ui 无 goal 事件消费 | `terminal-ui/launch.ts` | `tests/runtime-ui-parity.test.ts` | 同一 fixture 在 TUI/terminal 产生相同业务事件序列 |

### P0-5 发布可信度

| 要求 | 实现位置 | 测试入口 | 验收证据 |
|---|---|---|---|
| v0.1.1 sidecar 向后兼容读取 | `goal-storage.ts` | `tests/goal-storage-compat.test.ts` | 旧 sidecar 无损加载 |
| corrupt sidecar quarantine + 恢复建议 | `goal-storage.ts`、`services/doctor.ts` | `tests/goal-storage-compat.test.ts` | corrupt 不崩溃，提示恢复 |
| build/lint/全量 Jest/Goal E2E/PTY | 全局 | 全量 suite | 0 build error；goal E2E 通过 |
| tarball + 干净 prefix install + registry install | `package.json` | 手动 + `npm pack --dry-run` | 文件清单干净；`orion --version` |
| README/help/changelog 一致 | `README*.md`、`/help`、release note | 文档审计 | 无超前声称 |
| Go/No-Go + 回滚记录 | 本计划 §4 | — | 发布证据完整 |

## 3. P1 范围（原则上必须关闭）

| 项 | 要求 | 实现位置 | 测试入口 |
|---|---|---|---|
| P1-1 accounting | per-turn delta（非 session lifetime）、retry/fallback/subagent 计入口径、budget preflight、stop reason 分层 | `goals/accounting.ts`、`coordinator.ts` | `tests/goal-accounting.test.ts`(新) |
| P1-2 TUI 可见性 | 紧凑 status/inspector、audit failure 下一步、80/120/154 列、不破坏 input ownership | `tui-ui/*` | `tests/tui-*.test.ts` |
| P1-3 诊断 | `/doctor` 检查 orphan/corrupt sidecar（只读）、stale CAS/session mismatch 区分错误 | `services/doctor.ts`、`storage-maintenance.ts` | `tests/doctor`/storage tests |

## 4. Go / No-Go

### Go

- 原始 objective、显式约束、success criteria 跨 compact/restart/resume 恢复；
- continuation 全程 typed goal context，marker 不入 transcript；
- model tool 与 coordinator 按 session 隔离；
- `update_goal complete|blocked` 两阶段，runtime audit 才改变终态；
- 每个 passed criterion 有新鲜、相关、可追溯 evidence；failed/stale/missing 时保持 active；
- accounting 精确，达预算后零新增 provider call；
- pause/interrupt/replace/shutdown 后零 ghost continuation；
- TUI 清晰展示 goal 状态/下一步/预算/audit；terminal-ui/Print parity；
- v0.1.1 sidecar compat、corrupt recovery、rollback 通过；
- build/lint/全量测试/Goal E2E/真实 PTY/tarball/实际 PATH/registry install 全通过；
- PR/CI/review/merge/**tag**/npm/发布后验证各有证据。

### No-Go

- continuation 仍提交 marker 字符串或伪 user message；
- 任一 renderer 拥有私有 GoalCoordinator/audit/scheduler；
- `update_goal` 声称成功但 terminal request 未进入 turn outcome；
- completion 只查 evidence 数量或模型摘要；
- 旧/失败/无关/stale evidence 可关闭 criterion；
- session switch 后 tool 仍指向旧 coordinator；
- restart 后用户不可见时自动执行工具；
- TUI goal 展示破坏 input ownership/scrollback/CJK/paste/Ctrl+C；
- v0.1.1 sidecar 被隐式破坏性重写；
- 只报告 Jest 总量，无长会话/PTY/tarball/registry 证据。

## 5. 执行顺序与依赖

```
Phase 0  基线 + 矩阵（本文件）          ← 进行中
Phase 1  Contract + Storage + Accounting（P0-2 contract, P1-1）
Phase 2  Typed Turn + Scheduler + Resume（P0-1, P0-3）
Phase 3  Evidence + Terminal Audit（P0-2 audit）
Phase 4  Events + TUI + Parity（P0-4, P1-2）
Phase 5  Long-session + Compat + Release（P0-5, P1-3）
```

每个 Phase 有独立 Gate；P0 未关闭即 No-Go。
